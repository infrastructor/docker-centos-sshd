sudo: required

services:
  - docker
  
before_install:
 - docker login --username=$U --password=$P --email=$E
 
script: 
 - docker build -t infrastructor/centos-sshd:0.0.2 . 
 - docker push infrastructor/centos-sshd:0.0.2
