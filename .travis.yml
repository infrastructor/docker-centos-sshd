sudo: required

services:
  - docker
  
before_install:
 - docker login --username=$U --password=$P --email=$E
 
script: 
 - docker build -t infrastructor/centos-sshd . 
 - docker push infrastructor/centos-sshd
